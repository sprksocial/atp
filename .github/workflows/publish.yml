name: Publish

on:
  push:
    branches:
      - main

jobs:
  publish:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Clone repository
        uses: actions/checkout@v5

      - name: Set up Deno
        uses: denoland/setup-deno@v2
        with:
          cache: true
          deno-version: 2.x

      - name: Lint
        run: deno lint

      - name: Format
        run: deno fmt --check

      - name: Check
        run: deno check

      - name: Publish to JSR
        run: deno publish
